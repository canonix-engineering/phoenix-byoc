namespace: phoenix
imageRegistry: registry.test.invalid/phoenix

postgresql:
  bundled:
    enabled: true
  external:
    host: postgresql-postgresql
    port: 5432
    sslmode: disable

redis:
  bundled:
    enabled: true

cortex:
  bundled:
    enabled: true

application:
  tenantId: phoenix_test
  appHost: localhost
  allowedHosts: localhost,127.0.0.1
  cortex:
    host: cortex.test.invalid
  clickhouse:
    url: http://clickhouse.test.invalid:8123/phoenix_test
  mailer:
    from: Phoenix <noreply@test.invalid>
    domain: test.invalid
